apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: system:openshift:scc:helmguest-scc
rules:
- apiGroups:
  - security.openshift.io
  resourceNames:
  - helmguest-scc
  resources:
  - securitycontextconstraints
  verbs:
  - use
